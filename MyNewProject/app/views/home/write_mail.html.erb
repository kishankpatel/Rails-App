<div>
	<h2>Write mail here..</h2>
	<%= form_for @comment, url: "/send_mail/#{@user.id}", method: :post do |f| %>
		<%= f.text_field :message, class: 'input_text', required: true %>
		<%= f.submit 'Send' %>
	<% end %>
</div>